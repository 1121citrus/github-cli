#!/usr/bin/env bash

# github-cli: run the full test suite.
#
# Copyright (C) 2026 James Hanlon [mailto:jim@hanlonsoftware.com]
# SPDX-License-Identifier: AGPL-3.0-or-later

set -euo pipefail

whereami=$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")

status=0
"${whereami}/image-structure"   || status=1
"${whereami}/gh-invocation"     || status=1
"${whereami}/env-metadata"      || status=1
"${whereami}/shell-functions"   || status=1
exit $status
